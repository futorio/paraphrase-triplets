# HuggingFace модель https://huggingface.co/sberbank-ai/sbert_large_mt_nlu_ru
# Обязательные параметры:
#
# name: название модели, нужно для маппинга конкретного объекта
# seq_len: длинна последовательности токенов. Происходит truncate и padding до нужной длинны
#
# Необязательные параметры:
# path: путь до модели на диске, если не указан, модель будет скачана с hf хаба

seq_len: 24
# path: sberbank-ai/sbert_large_mt_nlu_ru

__val_schema__:
  seq_len:
    type: integer
    min: 5
  path:
    type: string
    required: false
